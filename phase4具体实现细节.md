1. 关于这个扩展机制设计，的确我现在还没有特别好的想法，我们先来进行商讨确定可行性和可拓展性和工程角度出发进行设计：
   目前在 GB26875 文档.md 中，可能出现用户自行定义的地方并不是很多，目前有控制单元的命令字节的值 128~255、应用数据单元的数据标识符结构中的类型标志的值 128~254、应用数据单元中的信息体结构中的系统类型标识符的值 128~255、应用数据单元中的信息体结构中的消防建筑设施部件类型的值 128~255、应用数据单元中的信息体结构中的模拟量类型（注意这里和前面的不同，其他都是类型值与类型值说明对应，这里是类型值与说明、单位、有效范围、最小计量单元唯一对应的，所以这里可能需要设计一下如何实现）；关于上面的内容是在进行编码或解析时用户（库使用者）需要进行补充的部分，我觉得设计成这种可插拔的拓展式是必须要有的功能。这里我的想法和 DEVELOPMENT.md 中想法差不多，但是有一部分其他感觉可能更好更通用的想法：

   - 首先用户自定义一个 struct 或 enum，我觉得最好是 enum，这个 enum 的结构类型于下面这样的：

     ```rust
     #[derive(应用数据单元类型标志定义表拓展)]// 每种定义表都要使用特定的过程宏去派生
     pub enum TypesExtended {
     UploadUserInformationTransmissionDeviceProductionDate = 128,
     ...
     }

     // 下面一定要实现这个trait，因为在进行解码（解析）的时候需要解释这里用户自定义的信息内容
     impl std::fmt::Display for TypesExtended {
     fn fmt(&self, f: &mut Formatter<'_>) -> Result {
        match self {
            TypesExtended::UploadUserInformationTransmissionDeviceProductionDate => "上传用户信息传输装置生产日期".to_string(),
            ...
        }
     }
     ```

     因为目前一共有五个可能的自定义类型值说明，其中还有一个比较特殊的（模拟量类型值还有包括对应的说明、单位、有效范围、最小计量单元，需要再考虑如何设计实现），然后就定义物种派生宏，这种派生宏就可以将自己定义的类型（包含了字段值信息）映射成对应的 map 类型，其中键为 u8，值为 String，然后就可以使用这个 map 类型来存储和访问用户自定义的信息内容。

   - 然后将派生宏转换后的 map 类型与之前代码中定义的自定义字段绑定，然后就可以使用这个 map 类型来存储和访问用户自定义的信息内容。
   - 编码时用户可以通过自己定义好的类型去编码，但这里我还没有看到编码功能（只看到了解码功能），所以我想这里尽量与标准协议定义的字段信息编码时的操作 API 相同或相近（也可以通过宏来改善）；在实现扩展机制时，可以考虑使用 trait 来定义扩展接口，然后通过实现这个 trait 来扩展功能。这样可以使得代码更加灵活和可扩展。
     关于我的思路你有什么想法和建议吗？

2. 关于文档测试失败问题，我们先不要管，等开发彻底完成发布到 crates.io 上之前解决就可以了，这里的目标想法同步到 DEVELOPMENT.md 文件中。
3. 开发顺序，我希望你先专注于 Phase4 的完整实现，不过现在我们先讨论一下如何实现这个扩展机制再进行开发。4. 先不要管 builder 模块的警告，因为目前可能还没有彻底实现 builder 模块，所以暂时不需要考虑。
4. 注册时机：因为我想把当前的项目设计封装成一个库，方便用户因入库直接使用，所以我的想法是应该是那种惰性注册，即真正使用时才会注册，而不是再库初始化时注册。2. 冲突处理：在注册时，如果发现已经存在相同的类型名或字段名，则抛出错误或警告，并提供解决方法。例如，可以要求用户重新命名冲突的类型或字段，或者提供一个自动重命名的选项。3. 序列化支持：拓展类型应该是必须支持序列化操作的，我的这个项目是库类别的，而且你可以检查 Cargo.toml 文件中定义了很多 feature，其中一些可能与序列化相关，例如 serde、serde_json 等。在实现序列化支持时，可以考虑使用 serde 库来简化序列化和反序列化操作。因此我希望是如果这个库被引入时开启了这个序列化 feature，那么拓展类型也应该支持序列化操作，如果未开启则不支持序列化操作，这也是为了和用户操作编解码操作统一，因为编码或解码后的数据信息可能以不同形式发送出去，不过如果编码成协议指定的帧就不需要序列化，但是用户再编码成协议规定的帧类型时可能是直接从一个实现了 serde 序列化、反序列化操作的类型直接映射过来的（后续打算添加这样一个功能，不过还没有探究这个功能的可行性）。4. 性能要求：在实现拓展类型时，应该尽量避免不必要的性能开销，例如避免频繁的内存分配和拷贝操作，可以考虑使用引用计数、池化等技术来减少内存分配和拷贝操作的次数。同时，也应该考虑拓展类型在不同场景下的性能表现，例如在高并发、大数据量等场景下的性能表现。此外，还应该考虑拓展类型在不同平台下的性能表现，例如在嵌入式设备、服务器等不同平台下的性能表现。你做的对，给你点赞，如果你哪里有什么不确定需要和我对接的地方必须要告诉我，我给你提供指导、意见等信息；现在你还有什么疑问直接提出来，这是为了后面编码的确定性做准备防止路线不统一等问题。
5. 过程宏的实现方式，我希望你在当前项目中创建子 crate，用于实现过程宏，同时注意同步到 DEVELOPMENT.md 文件中的项目结构组织。2. 这里的惰性注册，我的想法是像标准库中的迭代器一样只有真正使用的时候才会去注册，避免不必要的性能开销；因为当用户使用这个库的时候直接在代码中硬编码拓展类型，而不是通过动态加载方式（目前不打算实现这样的加载方式），所以我觉得首次使用时注册一次就可以了。3. 我大概明白了你说的这个冲突监测是什么意思了：是不是说当用户在代码中引入我们这个库时同时编解码了不同的类型，例如 A 类型对应 A 拓展标准，B 类型对应 B 拓展标准，但是会注册到全局唯一的注册器中会导致冲突对吗？如果确实存在冲突，那么我们需要提供一个机制来解决这个问题，例如通过命名空间或者模块来隔离不同的类型，或者将对应的拓展类型注册到不同的注册器中，例如使用不同的模块或者命名空间来隔离不同的类型（这里你觉得怎么样或者能不能实现，因为你说的这种情况的确可能比较常见）。4. 模拟量类型的特殊设计，范围解析格式我希望两种格式都支持，因为许多库比如 serde 或 clup 也是这样设计的（你可以参考它们的实现代码）。5. 与现有 codec 系统的集成点，在保证不修改现有标准实现逻辑情况下，修改现有方法，向后兼容；我的想法是可以使用上面的过程宏去实现对应的 trait，然后做类似 C++种的偏特化处理，对没有拓展类型和有拓展类型的就能区分处理了，具体我不清楚能不能或者在 rust 中实现优雅不优雅，可以考虑使用 trait 对象或者泛型参数来实现。6. 错误处理策略，因为我上面在问题 3 中解释如果能解决掉问题 3 的话应该不会有注册冲突问题，如果出现注册冲突直接报错，让用户去修改。现在还有什么问题吗，有什么建议吗？
6. 子 crate 命名使用 gb26875_macros2. 是的，你说得对，除了使用模块路径最好还需要包含版本信息来进一步避免冲突。3. 是的，需要预建索引或使用高效的数据结构（你可以在 crates.io 上查找是否有符合我们目标的库，如果没有可以考虑自己实现一个），同时还需要考虑如何处理不同类型之间的转换和兼容性问题。4. 错误信息，发生注册冲突时，错误信息包含具体是那个文件下的那中拓展类型发生了冲突，最好是具体到字段信息。我对你的这个实现计划比较满意，你接下来就按照上面我们的对话信息和实现目标以及计划路线去一步步实现，同时把你的计划同步到 DEVELOPMENT.md（同时修改项目组织结构，因为计划上发生了改变）
   src/extension/├── mod.rs # 模块入口，重新导出 ├── traits.rs # 扩展 trait 定义（已存在）├── registry.rs # 扩展注册表（已存在）├── manager.rs # MacroExtensionManager 和全局管理 ├── macro_registry.rs # MacroExtensionRegistry 实现 ├── conflict_resolution.rs # 命名空间冲突解决 ├── statistics.rs # 扩展统计和信息收集 └── error.rs # 扩展专用错误类型和 codec 模块的更新

src/extension/├── mod.rs # 模块入口，重新导出 ├── traits.rs # 扩展 trait 定义（已存在）├── registry.rs # 扩展注册表（已存在）├── manager.rs # MacroExtensionManager 和全局管理 ├── macro_registry.rs # MacroExtensionRegistry 实现 ├── conflict_resolution.rs # 命名空间冲突解决 ├── statistics.rs # 扩展统计和信息收集 └── error.rs # 扩展专用错误类型和 codec 模块的更新都要同步到 DEVELOPMENT.md 文件中我同意的拆分方案，先完成模块拆分，然后再解决编译错误，但是注重重构结构，但是你一定切记不能简化任何功能（可以新增功能）

GB26875 文档.md 是这个通讯协议的说明文档，整个开发过程的细节知道唯一参考这个文件中的内容；DEVELOPMENT.md 文件时我们本次想要实现的目标以及开发进度和开发技术路线等内容，你在开发时需要按照这个文件中给出的开发路线进行开发而且项目的组织结构也要按照这个文件中定义好的去实现。其他两个文件记录了当前的开发进度以及上一次的开发过程，现在你需要完整仔细阅读这四个文件，然后把它彻底理解明白，后面我会给你其他指令要求的
