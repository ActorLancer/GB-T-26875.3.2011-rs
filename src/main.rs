// This is a placeholder main.rs file for development purposes
// The actual library code is in lib.rs

fn main() {
    println!("GB26875 library - this should be built as a library crate");
    println!("Use `cargo build` to build the library");
    println!("Use `cargo test` to run tests");
    println!("Use `cargo doc --open` to view documentation");
}
